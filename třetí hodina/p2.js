vek = 50;

if (vek > 18) {
    // bude alkohol (tohle vynech)
    if (vek > 55) {
        // kvalitní alkohol pro seniory
    } else if (vek > 30) {
        // něco pro střední věk (30-55)
    } else {
        // něco pro 18-30 let
    }

} else {
    // nebude alkohol
    if (vek > 10) {
        // kvalitní limonáda pro teens
    } else if (vek > 5) {
        // něco pro 5 - 10 let
    } else {
        // něco pro děti do 5 let
    }
}